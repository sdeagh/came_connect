---
phase: 01-protocol-discovery
task: 2
total_tasks: 2
status: blocked
last_updated: 2026-03-15T23:14:18.098Z
---

<current_state>
Phase 1 remains blocked on protocol discovery. The strongest current result is no longer just "the official app opens the door"; it is now confirmed on both iPhone and Android identities:

- iPhone path: `srcaddr=00e70000` -> Mobile App 1 / `00700100000`
- Android emulator path: `srcaddr=00e70001` -> Mobile App 2 / `00700100001`

The Android emulator app successfully opened the door and the panel-side chain was confirmed again:

- `OPEN_DOOR_IND` at panel UI with `srcaddr=00e70001`
- `OPEN_DOOR_REQ` in `lpcmanager`
- serial write `8202190000831B1B`
- immediate ACK from X1

However, direct cloud reproduction is still blocked:

- `xipregister(00700100001)` -> HTTP `403`
- SIP `REGISTER` for `00700100001` -> `401 Unauthorized`
- this still fails even with:
  - Android-shaped `xipregister` metadata
  - observed Android FCM token
  - rotated local `md5secret` / HA1 `ef769e587ac3ed1e24eb73f04cf18479`

Current conclusion: the working app path depends on auth material or signaling beyond the local `md5secret` and beyond the currently modeled `REGISTER + MESSAGE` flow.
</current_state>

<completed_work>

- Task 1: Local and cloud protocol sweep - Done
- Task 2: Documentation and blocker analysis - Done, but phase remains blocked
- Post-plan exploration: Android emulator setup - Done
- Post-plan exploration: Installed and verified `com.came.myaccess` on emulator - Done
- Post-plan exploration: Pulled APKs for analysis
  - `tmp/android_apk/com.came.myaccess-base.apk`
  - `tmp/android_apk/com.came.myaccess-split-arm64.apk`
  - `tmp/android_apk/com.came.myaccess-split-en.apk`
- Post-plan exploration: Confirmed Android app runtime logs expose:
  - `Execute feature command: Open door`
  - `SendOpenDoorCommand`
  - local SIP identity `00700100001@2B8EED6C49FCD387.xip.cameconnect.net`
  - remote SIP target `00800000000@2B8EED6C49FCD387.xip.cameconnect.net`
- Post-plan exploration: Updated `tools/sip_cloud_opendoor.py` so `xipregister` metadata is configurable by env
  - `PUSH_DEVICE_TOKEN`
  - `VOIP_DEVICE_TOKEN`
  - `APP_NAME`
  - `OS_TYPE`
  - `APP_LANG`
  - `HTTP_USER_AGENT`
- Post-plan exploration: Updated docs and state with Android findings
</completed_work>

<remaining_work>

- Extract the missing auth/signaling detail that lets the official app register and send the working door-open flow
- Prefer one of these next:
  - runtime instrumentation on Android
  - controlled clean login trace + clean single door-open trace
  - deeper static extraction of MAUI assembly store / managed code
- If resuming with the capture approach, do not reuse the earlier long-running adb sessions; use short bounded snapshots only
- If resuming with instrumentation, install:
  - Android build tools (`build-tools;35.0.0`)
  - `frida-tools`
  - then pursue a gadget/repack path because:
    - `adb root` is unavailable
    - `run-as` fails (`package not debuggable`)
</remaining_work>

<decisions_made>

- Treat `00401200000` as the internal receiver account only; it is not the cloud-mobile identity
- Treat `00700100000` and `00700100001` as confirmed mobile cloud identities tied to `00e70000` and `00e70001`
- Treat the local `md5secret` as insufficient by itself for cloud reproduction
- Keep `tools/sip_cloud_opendoor.py` as the live cloud repro harness and extend it rather than creating more throwaway scripts
- Prefer Android over iOS for further reversing because logs are richer and runtime instrumentation is more practical
- Avoid more blind "press again" loops; use one controlled action per log capture
</decisions_made>

<blockers>
- Local SIP blocker: Asterisk still returns `403` for plain-text LAN `MESSAGE` and `415` for JSON variants
- Local loopback blocker: `callmanager` on `5062` is not reachable from LAN
- Cloud auth blocker: both `00700100000` and `00700100001` fail direct cloud repro despite known local password rotations
- Android capture blocker: earlier attempts with long-running adb logging were noisy and brittle; use bounded dumps next time
- Instrumentation blocker: emulator is production, no `adb root`, app is not debuggable, so plain `frida-server`/`run-as` path is unavailable
</blockers>

<context>
Mental model at pause:

The key breakthrough was identity mapping, not direct control. The Android emulator proved a second working app identity (`00700100001` / `00e70001`) and showed the app-side method names. That narrows the problem substantially: the missing piece is probably not "which SIP user?" anymore. It is "what extra auth material or runtime-generated registration state does the official app have that our standalone script lacks?"

Static APK shape is important:

- base APK has `classes.dex`, `classes2.dex`, `classes3.dex`
- arm64 split contains:
  - `libaot-MyAccess.Core.dll.so`
  - `libaot-MyAccess.Data.dll.so`
  - `libaot-MyAccess.dll.so`
  - `liblinphone.so`
  - `libmonodroid.so`
  - `libassemblies.arm64-v8a.blob.so`

So this is still a .NET MAUI AOT app with native and managed pieces. Plain `strings` on the AOT libs was not enough to recover methods cleanly. The external storage directory for the app was basically empty. `adb root` failed and `run-as` failed, so runtime inspection likely requires gadget-based instrumentation or repackaging.

There was an attempted restart of a controlled Android capture after rebooting the emulator, but it was intentionally paused by the user before completion. Current status of that attempt:

- emulator rebooted cleanly
- app was relaunched
- no trustworthy new login trace or command trace was saved yet
- `tmp/android_capture/came-login.log` / `came-door.log` should be treated as disposable unless re-captured cleanly
</context>

<next_action>
Start with one of these two paths:

Path A: Controlled capture restart
1. Reboot emulator if needed
2. `adb shell am force-stop com.came.myaccess`
3. `adb logcat -c`
4. launch app
5. park on main screen
6. take a short bounded filtered login snapshot, not a long-running logcat session
7. clear logcat again
8. do exactly one door-open press
9. take a short bounded filtered command snapshot
10. immediately pull fresh `systemlogs.zip` from panel and correlate timestamps

Path B: Android instrumentation
1. Install `frida-tools`
2. Install Android `build-tools;35.0.0`
3. Choose gadget/repack route for `com.came.myaccess`
4. Target the registration/auth path around:
   - `LinphoneService`
   - `BaseApiService`
   - `SendOpenDoorCommand`
   - token / secure storage retrieval

If uncertain, resume with Path A first because it is lower-risk and may still expose the missing bootstrap sequence without patching the app.
</next_action>
