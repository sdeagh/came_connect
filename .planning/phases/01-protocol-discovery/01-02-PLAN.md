---
phase: 01-protocol-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tools/sip_door_test.py
  - docs/sip-protocol.md
autonomous: false
requirements:
  - RE-01
  - RE-02
  - RE-03
  - RE-04

must_haves:
  truths:
    - "At least one approach physically opens the door (confirmed by user observation)"
    - "The winning approach is documented with exact headers, body, and sequence"
    - "tools/sip_door_test.py runs the proven command with CLI args and exits 0 on success"
  artifacts:
    - path: "tools/sip_door_test.py"
      provides: "Final clean script running only the proven approach"
      min_lines: 100
    - path: "docs/sip-protocol.md"
      provides: "Protocol documentation: headers, body, auth, sequence"
      min_lines: 30
  key_links:
    - from: "tools/sip_door_test.py"
      to: "XTS7 device"
      via: "Proven SIP MESSAGE approach"
      pattern: "def open_door"
---

<objective>
Run the investigation script against the real device, identify which approach opens the door, then produce the final clean test script and protocol documentation.

Purpose: This is the core discovery work — iterating through approaches with physical verification until the door opens. The output is a proven, documented protocol.
Output: Updated tools/sip_door_test.py (clean, single-approach), docs/sip-protocol.md (protocol reference)
</objective>

<execution_context>
@/home/d0m/.claude/get-shit-done/workflows/execute-plan.md
@/home/d0m/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-discovery/01-CONTEXT.md
@.planning/phases/01-protocol-discovery/01-RESEARCH.md
@.planning/phases/01-protocol-discovery/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run investigation approaches and identify working command</name>
  <files>tools/sip_door_test.py</files>
  <action>
CHECKPOINT — Human physical verification required.

Plan 01 created tools/sip_door_test.py with all 6 investigation approaches, correct HA1 credentials, and CLI args. The user must run the script near the door and report which approach physically opens it.

Present the following instructions to the user:

Run the approaches in order. Stand near the door to observe physical movement. Stop as soon as one works.

Step 1 — Approach 1 (most likely to work based on research):
  python3 tools/sip_door_test.py --approach 1
Watch for: SIP 200/202 response AND door physically opens.

Step 2 — If approach 1 gets 200/202 but door does NOT open, try approach 2:
  python3 tools/sip_door_test.py --approach 2

Step 3 — If no luck, try approach 3 (JSON body):
  python3 tools/sip_door_test.py --approach 3

Step 4 — If still nothing, try approaches 4 and 5:
  python3 tools/sip_door_test.py --approach 4
  python3 tools/sip_door_test.py --approach 5

Step 5 — If all local approaches fail, try the cloud fallback:
  python3 tools/sip_cloud_opendoor.py

Ask user to report:
1. Which approach number worked (or "none")
2. The exact script output for the working approach
3. If none worked, paste the output of --approach all so we can diagnose

Resume signal: "Approach N worked" with output, OR "None worked" with full output.
  </action>
  <verify>User reports which approach opened the door (or that none did)</verify>
  <done>User has confirmed which SIP approach physically opens the door, or confirmed all local approaches failed</done>
</task>

<task type="auto">
  <name>Task 2: Clean up script and document protocol</name>
  <files>tools/sip_door_test.py, docs/sip-protocol.md</files>
  <action>
Based on the user's report from Task 1:

**If a local approach worked (approaches 1-5):**

1. Update tools/sip_door_test.py:
   - Remove the multi-approach investigation logic
   - Keep only the proven working approach as the default behavior
   - Retain CLI args: --device-ip, --sip-user, --ha1, --to-user
   - Add clear exit codes: 0 = door command sent successfully (200/202 response), 1 = error
   - Keep the comprehensive docstring, updated to reflect the proven approach
   - Print a clear success/failure message

2. Create docs/sip-protocol.md documenting:
   - The working SIP MESSAGE format (exact headers)
   - Body content and content-type
   - Authentication method (digest with HA1 mode)
   - The full request/response sequence
   - Device addressing (which TO user, which port)
   - Known credentials and how they were discovered
   - Which approaches were tested and their results
   - The door-open signal chain (SIP MESSAGE -> Asterisk -> callmanager -> lpcmanager -> serial -> X1 -> door)

**If no local approach worked (fallback to cloud):**

1. Update tools/sip_door_test.py to document that local approaches failed and why
2. Create docs/sip-protocol.md documenting:
   - All local approaches tested and why each failed
   - The cloud SIP path as the working alternative
   - Reference to tools/sip_cloud_opendoor.py as the proven approach
   - What would need to change for local to work (e.g., firmware update, SSH access to modify dialplan)

**In either case:** The script must accept --device-ip, --sip-user, and --ha1 as CLI args (no hardcoded device-specific values per RE-04).
  </action>
  <verify>
    <automated>cd /home/d0m/Projects/gtapps/came_connect && python3 tools/sip_door_test.py --help && test -f docs/sip-protocol.md && echo "OK"</automated>
  </verify>
  <done>
    - tools/sip_door_test.py runs the proven command (single approach, not investigation mode)
    - CLI args: --device-ip, --sip-user, --ha1 (no hardcoded device values)
    - docs/sip-protocol.md documents the complete protocol with headers, body, auth sequence
    - Exit code 0 on success, 1 on error
  </done>
</task>

</tasks>

<verification>
- `python3 tools/sip_door_test.py --help` shows clean CLI (not investigation mode)
- `docs/sip-protocol.md` exists with protocol documentation
- Running `python3 tools/sip_door_test.py --device-ip 192.168.1.88` physically opens the door (user verified in Task 1)
</verification>

<success_criteria>
- The door physically opens when running the script (confirmed by user)
- The winning approach is identified and documented
- tools/sip_door_test.py is a clean, single-purpose script (not a multi-approach investigator)
- docs/sip-protocol.md captures the full protocol for Phase 2 integration work
- No hardcoded device-specific values in the script
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-discovery/01-02-SUMMARY.md`
</output>
